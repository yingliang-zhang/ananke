Resume exact session `019f7300-f009-7000-8963-9f08552ade1e`. Continue implementation; do not restart.

Current verified state after orchestrator fixes:
- 29 Go files, 5,534 LOC, 48 test functions.
- ALL gates pass: standard, race, no-CGO, vet, all binary builds.
- Phase 1-5 complete: store, lifecycle backend, supervisor binary, fake worker, daemon+API.
- Orchestrator fixed: reentrant mutex deadlock, fakeworker pre-exit delay, empty payload JSON marshal, GetRunState timing.

Remaining work — Phase 6 (mutation gates) and Phase 7 (verification scripts):

### Phase 6: Mutation gates

Create build-tag-controlled mutation files under `internal/`:
- `internal/lifecycle/mutation_reap_before_cleanup.go` (tag: `mutation_reap_before_cleanup`)
- `internal/store/mutation_no_outbox.go` (tag: `mutation_no_outbox`)
- `internal/lifecycle/mutation_signal_after_reap.go` (tag: `mutation_signal_after_reap`)
- `internal/lifecycle/mutation_terminal_while_alive.go` (tag: `mutation_terminal_while_alive`)
- `internal/store/mutation_reset_offset.go` (tag: `mutation_reset_offset`)
- `internal/lifecycle/mutation_cancel_parent_only.go` (tag: `mutation_cancel_parent_only`)

Each mutation file uses a build tag to alter production behavior when that tag is active. Each mutation must have a named test (in a `_test.go` file with the same build tag) that:
- PASSES in baseline (no mutation tag)
- FAILS only because of the mutation's behavioral change (not compile/setup error)

Write `scripts/mutation_proof.py` that:
- Runs baseline (no tags): all named mutation tests pass.
- For each mutation: build with the tag, run the named test, verify it fails.
- Classifies: `behavioral_test_rejection` (expected) vs `compile_failure` or `timeout` (unexpected).
- Outputs `reports/mutation-proof.json` with per-mutation results.

### Phase 7: Verification scripts

Write `scripts/verify.py`:
- gofmt check on all Go files
- go vet
- go test (standard)
- go test -race
- CGO_ENABLED=0 go test
- CGO_ENABLED=0 go build ./cmd/...
- Output `reports/verification.json` with pass/fail per gate.

Write `scripts/stress_lifecycle.py`:
- 20-pass crash/restart: launch run, SIGKILL daemon, restart, verify recovery.
- 20-pass cancellation: launch run, cancel, verify cancelled state.
- Stop on first failure.
- Output JSON reports.

Write `scripts/blackbox_lifecycle.py`:
- Build production binaries (ananke, ananke-supervisor, ananke-fakeworker).
- Drive the local API through CLI (subprocess calls).
- Verify no survivors for cancellation and successful completion.
- Output JSON reports.

Keep all work inside `/Users/yingliangzhang/Projects/ananke/`. Do not modify ADRs or the implementation contract. `go fmt ./...` before stopping. Report exact evidence.
