Resume exact session `019f7300-f009-7000-8963-9f08552ade1e`. Continue implementation; do not restart.

Current verified state:
- Phase 1 (store): COMPLETE. `go test ./internal/store/ -count=1` passes. 18 Go files total.
- Phase 2 (lifecycle backend): IN PROGRESS. `backend.go` has BecomeGroupLeader, SignalProcess, ProcessAlive, GroupMembers implemented. Missing: LaunchWorker, WorkerExited, ReapWorker. The compile-time assertion `var _ LifecycleBackend = (*DarwinBackend)(nil)` fails because of missing methods.

Immediate fix: implement LaunchWorker, WorkerExited, ReapWorker in backend.go. Then run focused lifecycle tests.

Continue through Phases 3-7 as described in the original prompt. Work as fast as possible — prioritize correctness of the supervisor lifecycle, cleanup state machine, and daemon recovery. Mutation gates and verification scripts are Phase 6-7.

Keep all work inside `/Users/yingliangzhang/Projects/ananke/`. Do not modify ADRs or the implementation contract. `go fmt ./...` before stopping.
