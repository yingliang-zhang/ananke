Repair remaining P1b focused review blockers from artifacts/omp/p1b/focused-rereview-output.md. Do not commit/push or add P2/P3/UI.

Implement a NEW migration v9 (do not mutate applied v8) that rebuilds the relevant tables with reciprocal full-tuple DEFERRABLE INITIALLY DEFERRED Approval -> RevisionLifecycle FK: (proposal_id,revision,revision_hash,approval_id). Maintain all prior identity constraints/data. Make GetApproval validate exactly one matching lifecycle pair and return ErrProposalRecordCorrupt for FKs-disabled reassignment/orphan. Add tests: FKs-enabled orphan approval insertion rejected; FKs-disabled lifecycle reassign/orphan GetApproval rejected; foreign_key_check clean after rejected writes and detects intended bypass corruption. Replace the fake v7->v8 test fixture with actual historical v7 DDL (pre-v8 composite constraints), insert valid v7 chain, Open migrates through v8/v9, then assert constraints/replay/foreign_key_check. Strict RED->GREEN; run full/race store and P1a verifier; factual ledger evidence only.