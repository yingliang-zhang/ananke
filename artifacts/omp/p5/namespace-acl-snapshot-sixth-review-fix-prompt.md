Fix sixth-review HIGH 2 in `/Users/yingliangzhang/Projects/ananke-p0a-schema-codegen`. Strict TDD. No commit, no docs/ledger, and do not edit `internal/releaseartifact` or release CLI. Scope: `internal/trustedsupervisor/**` and, only if required, same command's server factory tests.

Read `artifacts/omp/p5/sixth-review-report.md` finding 2 and current namespace/snapshot code. Close both linked correctness gaps:
1. Production `stable` namespace must never be inferred from Unix mode bits alone on Darwin. Inspect every physical ancestor and configured namespace root for extended ACL authority. A conservative fail-closed policy that rejects every nontrivial/extended ACL is acceptable and preferred over incomplete effective-permission emulation. Use a native, deterministic Darwin API; no locale-dependent `ls` parsing, shell command, environment flag, or production bypass. Keep non-Darwin fail-closed. Compile-time test authority may use explicit test-only behavior but production binary must not contain runtime bypass knobs.
2. Prove runtime UID/GID and inherited groups have no ability to add, delete, rename, or write under accepted ancestry. If full native ACL semantics cannot be proved, reject the hierarchy. Document behavior in code comments only; docs lane is separate.
3. Make snapshot staging root creation, source child creation, tar extraction, file/dir publication, chmod/sync, and final capture descriptor-relative through retained work-root authority. Do not reopen absolute work paths to authorize changes. Reads of immutable repository source may remain separately pinned by existing policy, but all supervisor-owned output writes/publishes must be `*at`/FD-relative.
4. Durable signed owned-root evidence and finalizing recovery must treat `ENOENT` as prior cleanup only when the stable-parent proof above holds. ACL-authorized rename-without-decoy must be rejected at admission; identity mismatch remains nonterminal.
5. RED/GREEN tests: Darwin ACL on an ancestor/root granting named user/group/everyone or inherited add/delete/write authority; ACL-authorized rename-without-decoy live/restart; snapshot work-root pathname swap at each staging/publication window; descriptor still targets original; production build has no test bypass. Tests may skip only native ACL creation where unavailable, with a separate deterministic unit seam proving rejection.
6. Re-run existing Phase B decoy/path-only/multi-attempt/callback matrix count=10 and race=3, full trustedsupervisor package, gofmt/vet/diff-check.

Do not broaden into repair execution. Return exact changed files, tests/results, and any unresolved OS API limitation.