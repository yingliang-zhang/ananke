Repair ONLY blockers 1-3 plus atomic process readiness from /tmp/ananke-p5-read-only-executor-hard-review-output/review-output.md. Strict TDD; no commit/push; fake wrapper only; do not redesign evidence/journal/resume/cancellation semantics except cleanup needed here.
1) Execution-policy content authority: retain canonical bytes/content hash/size and re-read+rehash at every effect boundary, detecting in-place same-inode rewrite. Closed schema.
2) Immutable exec boundary on Darwin: open wrapper O_NOFOLLOW, fstat/hash that exact fd, retain fd through Start, and execute the script via root-owned immutable /bin/bash + inherited /dev/fd/N (Cmd.ExtraFiles), never by validated pathname. Test true replacement BETWEEN validation and Start and in-place rewrite. Restrict production Git to root-owned non-writable system executable (/usr/bin/git or equally immutable system path) and pin/recheck root owner/device/inode/hash; do not path-exec arbitrary user Git. If Git must be descriptor-run, implement it; fail closed rather than check-then-path. Preserve fake tests through test-only injection without weakening production.
3) Sandbox least authority: replace file-read*/process*/unrestricted mach/network. Execution policy must declare closed read roots/executable roots and exact route-required credential env names from a global allowlist; inherit ONLY selected route credentials. Explicitly deny server private-key/trust/policy/repository-policy/execution-policy/journal/WAL/SHM paths, parent argv/process inspection/signalling, unrelated local Unix/TCP endpoints. Allow only snapshot + pinned runtime/config read roots, isolated write roots, needed fork/exec, DNS/API TLS outbound (bounded as platform supports). Add adversarial fake tests attempting to read signing key/policies/journal/unrelated home, inspect/signal parent, connect local socket, write source/original; all fail while fake API/write-root happy path passes.
4) Credential/tree cleanup: scan on every exit. On nonzero/cancel/malformed/evidence rejection, securely remove prompt/output/session/tmp/work trees. Timeout may preserve session only after bounded secret/path scan passes; secret-bearing timeout must scrub and stop fail-closed. Inherit only route credential names. Tests assert secret-bearing output/session files are actually absent after failures/timeouts, not just absent from journal.
5) Publish process_e2e test readiness atomically (temp close+rename) to eliminate empty JSON race.
Run focused repeated/race/full package/vet. No real OMP, no evidence marker redesign, no resume/cancel redesign.