Resume exact releaseartifact session 019f9da0-5adf-7000-aa65-a9da1de77b62. Fix only the two HIGH findings in `artifacts/omp/p5/seventh-review-report.md`. Strict TDD, no docs/ledger/commit, no edits outside internal/releaseartifact and release CLI.

HIGH 1: retained Go/repository descriptors are checked but exec still uses mutable `goTool.path` and `repositoryRoot`; pre/post checks miss ABA replacement. Add deterministic RED tests that replace the actual compiler pathname or repository pathname after the last prelaunch check, allow the substituted build attempt, restore the original before post-build checks, and prove BuildAndPublish fails with no output. Then implement a Darwin-native fail-closed launch mutation guard bound to retained compiler and repository objects (for example EVFILT_VNODE/kqueue events for write/delete/extend/attrib/link/rename/revoke, registered before process launch and checked after exit and before publish), plus identity/hash checks. The test must prove the replacement really reached the build interval, not merely install an unused fake. Non-Darwin must fail closed if equivalent proof is unavailable. Do not claim FD execution on Darwin.

Also consider in-place compiler changes and repository directory entry changes; monitor sufficient retained objects/parents so any pathname substitution required to run another compiler or repository produces a sticky observed event even after restoration. Build candidate must never publish once the guard reports mutation.

HIGH 2: Mkdirat then Openat can record a replacement staging directory as owned and later delete it. Prefer removing the staging directory entirely: create one random hidden candidate file directly under the already pinned output-directory FD with Openat(O_RDWR|O_CREAT|O_EXCL|O_NOFOLLOW), retaining the returned FD and created identity atomically; build into that FD; verify it; renameNoReplace within the same pinned output directory; clean only that hidden name when it still matches the atomically created identity. If retaining a directory, provide equally strong non-substitutable ownership; a post-Mkdirat stat is not sufficient.

Add the exact regression where the would-be stage is renamed/replaced between creation and opening and prove the foreign replacement survives. Update prior tests to the simpler direct candidate design without weakening existing sentinel/overlay/path/inode/hash/no-replace coverage. Run package+CLI count=1, focused count=10, race count=3, vet, gofmt, diff-check. Return exact results and unresolved limits.