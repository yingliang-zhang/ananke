Resume exact session 019f9a4a-a904-7000-b341-e07ecf0e3baf for bounded integration repair only. Current executor files compile, but focused regressions:
- TestProductionServerMakesReconcileAndCancelReceiptExclusive: Reconcile returns nil,nil while audit still running; Cancel can close with incomplete frame.
- TestProductionServerSeparateProcessClientSubmitCrashRestartReconcileCancel: restart Reconcile returns nil,nil while audit still running.
Do not invent terminal success or restore old audit_not_run callback. Make running semantics explicit: Reconcile while prepared/running returns a canonical nonterminal response without persisting a terminal callback/operation conflict; tests must assert this. Existing terminal callback/cancel exclusivity must be tested after deterministically waiting for audit terminal state. Cancel during running must synchronously own/kill/reap the PGID, persist terminal cancellation/evidence, and ALWAYS return a complete signed response or explicit canonical error—not EOF/incomplete frame. Restart tests must wait/poll via bounded protocol until terminal, then verify signed callback. Update old tests without deleting security assertions. Finish focused executor/server tests, repeated/race, and report. No new features, no real OMP, no commit/push.