Repair the REJECTED P5 local trusted-supervisor transport. Strict TDD; do not commit/push; do not add OMP/server/repair yet. Read /tmp/ananke-p5-local-transport-normal-prereview-output.md fully and the frozen P3f protocol-adapter contract/fixtures first. Address all blockers, not symptoms:
1) Remove production PinnedLocalAuthentication acceptance. Implement real Ed25519 (or equally standard Go-stdlib) detached signature verification with pinned public trust roots, release attestation + independent approval + typed MoA grant and active/successor/revoked lifecycle validation at delivery/receipt/callback issued_at. No private keys in Ananke/client/evidence.
2) Make local wire conform to frozen P3f adapter chain: closed records and exact transitive bindings, separate delivery/receipt/callback nonce/channel/signature evidence, validity intervals, release/approval/MoA roots. Add delivery authentication.
3) Reconcile/cancel must bind the exact durable envelope and all release pins; change lifecycle seam/store types if required. No fresh CLI pin drift can be accepted.
4) Close socket-path TOCTOU security consequence: connected peer must prove key possession; validate connected peer credentials/PID as defense in depth. Path Lstat alone is never authority.
5) Persist nonce/channel/signature/replay authentication evidence transactionally with receipt/callback/cancellation so restart cannot reset transport replay policy; exact replay may return durable result without nonce reuse, conflicting reuse rejects.
6) Authentication hooks and all exchange work obey the same <=10s context deadline.
7) Add real lifecycle/store integration through full fence plus command submit/recover/cancel tests; cover same-UID impostor, path replacement, release pin drift, root rotation/revocation/expiry, signature drift, callback/cancel conflict, restart replay, unknown/partial frame/boundaries.
Keep socket path/operator credentials out of sealed wire/evidence. Preserve waiting_for_human and no Run/OMP/source/artifact access. Run focused/full/race/vet/P3d-P4 contracts and ledger. Return exact RED/GREEN evidence and residual risks.