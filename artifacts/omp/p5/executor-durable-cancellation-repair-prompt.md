Repair ONLY hard-review blocker 6: durable cancellation semantics. Strict TDD; no commit/push; fake wrapper only; preserve authority/sandbox/evidence/resume/journal invariants.
Required architecture:
1) Two-phase durable cancellation. After full request authentication, transactionally persist an immutable cancellation intent binding request hash/bytes, envelope/handoff/receipt/cancellation hash, attempt, expected PID/PGID/start identity if known, and state=requested BEFORE any signal. Commit it. Conflicting request/nonce/receipt/cancellation rejects before effect.
2) Exact replay: before any signal, if the identical cancellation intent is completed, return the original byte-identical signed acknowledgement from durable storage. A requested-but-incomplete intent after crash must resume cancellation idempotently, never issue an unrelated new response.
3) Pre-start window: cancellation requested while audit is materializing/prepared or before active.cancel installation prevents Start and transitions to confirmed cancelled without launching. Executor must consult durable cancellation intent at every effect boundary.
4) Running active/restarted process: verify exact PID+PGID+start identity ownership, send bounded TERM to owned group, wait bounded, then KILL if needed, wait bounded, inspect and require confirmed exit. Do not ignore signal/wait/inspect errors. Never append cancelled or acknowledge unless group exit/non-start is proven.
5) After confirmed cancellation, transactionally append exact audit cancelled evidence and generate/store signed cancellation acknowledgement + possession authentication, marking cancellation intent completed atomically with canonical response. Then write full frame. Crash at requested/after-signal/after-audit-event/before-response must recover idempotently.
6) Failure/conflict responses must always be complete bounded canonical frames. Add a signed/possession-authenticated typed error response if necessary so Client can distinguish authenticated cancellation failure from truncated/forged rejection; no bare unauthenticated rejected frame accepted as success. Exact successful ack replay remains byte-identical.
7) Server.Close and executor.Close use same bounded ownership-confirming path and cannot hang. Restart cancellation of stale/wrong/reused PID fails closed to waiting_for_human, not false cancelled.
Tests: pre-start deterministic gate, cancel before active registration, active TERM success, TERM ignored→KILL, Kill error, unkillable/timeout, wrong/reused PID, concurrent duplicate exact cancel, conflicting cancel, crash after each phase, restart recovery, exact byte replay with dial/effect count zero, callback/cancel exclusivity, full frame on every path, Close bounded. Repeat/race/full package/vet. No real OMP, no other redesign.