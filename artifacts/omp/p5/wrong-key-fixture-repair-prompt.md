Fix ONLY the deterministic same-UID wrong-key path-replacement regression in internal/trustedsupervisor/process_e2e_test.go. Root cause proven: strict and impostor helper processes call the same deterministic key fixture, while client now correctly expects impostor PID; therefore impostor has the legitimate private key and is accepted. Strict TDD: create distinct deterministic key/root/trust identities for impostor mode (e.g. namespace the test seed/fixture), while preserving all frozen predecessor envelope values required by the request. Client must continue loading the legitimate public trust bundle and expect the connected impostor PID, so rejection proves lack of legitimate Ed25519 key possession—not PID mismatch. Add explicit precondition assertions that legitimate/impostor peer public key, SPKI, trust-bundle hash differ and no private data crosses. Run the exact test count=10 and race count=3, signed production server tests, focused package. Do not modify production, docs, or other packages. No commit/push.