Fix ONLY the final P5 test-matrix blocker. Do not change production unless a test reveals a real bug; no commit/push.
Extend internal/trustedsupervisor signed authorization/client tests table-driven so EVERY forbidden category required by P3f—raw authority, credential, secret, key/private-key/key-marker, command, argv, environment, source, artifact, evidence, path/URL, whitespace—is exercised as a genuinely self-hashed and Ed25519-signed ReleaseApproval.ApprovalID AND MoARoleGrant.GrantID. For each forged signed authorization, prove Client Deliver, Reconcile, and Cancel reject before dialing (dial-call count remains zero). Do not satisfy by passing synthetic opaque_value JSON directly to scanner. Retain recursive scanner tests as defense-in-depth. Keep runtime bounded by reusing fixtures/table loops. Run focused test count=3 and race, signed process E2E, full compile/vet if time. No production server/OMP.