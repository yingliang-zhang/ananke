Repair ONLY production server P1 findings #1 and #2 from /tmp/ananke-p5-production-server-hard-review-output/review-output.md. Strict TDD; no commit/push; do not touch wire protocol, server Close/admission, key loader, docs, OMP.
A) Close SQLite URI/path pinning. Choose the smaller robust policy: reject absolute journal paths containing SQLite URI metacharacters (?, &, %, # and any delimiter modernc URI parsing can reinterpret), OR correctly percent-escape and verify SQLite's actual main database filename/identity against the pinned O_NOFOLLOW fd. Tests must prove valid macOS filenames with ?mode=memory&, %, # cannot redirect or lose restart durability; symlink/replacement remains fail closed.
B) Authenticate journal content and schema. On startup AND every replay/load, recompute and compare request_bytes_hash/response_bytes_hash before returning bytes. Validate all durable receipt/callback/cancellation records/fingerprints transitively. Pin exact canonical sqlite_master definitions (tables, indexes, triggers) or a canonical schema inventory hash—not just names/counts. Detect same-named no-op/altered immutability triggers.
C) Add deterministic tests: mutated request bytes, response bytes, hash columns, future migration, gapped history, altered table/index/trigger SQL, same-name no-op trigger, integrity_check-clean semantic corruption, exact restart replay still byte-identical. Fail closed before signing/replaying.
Preserve current exact replay/transactions. Run focused journal tests repeated/race, full package/vet if time. Report RED/GREEN and residual findings #3/#4 separately.