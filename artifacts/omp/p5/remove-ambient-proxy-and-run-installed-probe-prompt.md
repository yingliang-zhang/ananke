Mechanical production cleanup + verification only. In auditInvocationEnvironment remove HTTP_PROXY, HTTPS_PROXY, ALL_PROXY, NO_PROXY entirely. Explicit isolated models.yml baseUrl is the sole transport routing authority; gateway validation must not require proxy env. Update exact env tests if needed but keep those keys forbidden. Run full package. Then run `ANANKE_PINNED_OMP_FIXTURE=/opt/homebrew/Cellar/omp/17.1.3/bin/omp go test ./internal/trustedsupervisor -run '^TestAuditInstalledOMPProviderFreeTransportPreflight$' -count=10 -v -timeout 300s`; it must RUN, not skip, and make no real provider call/credential leak. If the exact installed probe fails, report real request/config error and fix only the provider-free harness or models.yml format, not bypass/skip. No commit.