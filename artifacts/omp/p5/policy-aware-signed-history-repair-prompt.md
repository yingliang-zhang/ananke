Repair ONLY third-review residual 2: policy-aware history authority and correlated resealed tamper. Read /tmp/ananke-p5-read-only-executor-third-hard-review-output/review-output.md. Strict TDD; no real OMP; no commit/push.
Threat: attacker rewrites every related audit row/evidence consistently and recomputes ordinary hashes. Internal continuity alone is insufficient.
1) Bind serverJournal to the already pinned executionPolicy and current server Ed25519 signing authority before audit recovery. After binding, revalidate every existing audit intent/event/cancellation outcome. Production append/load/startup/cancel/reconcile/replay must fail closed if authority absent for audit rows.
2) Policy-aware validation: resolve exact entry by intent LaunchSpecHash and require PolicyHash/task/route/repo/Git/archive/wrapper/cap equality. For each attempt, record the minimal sealed resume facts needed to recompute auditCommandDescriptorHash (prompt SHA, resume UUID, synthesize flag/session run ID) and recompute exact descriptor from policy—not continuity only. Derive exact paths from entry.WorkRoot/PromptRoot/OutputRoot/SessionRoot/TemporaryRoot + intent RunID + attempt; exact equality, no basename-only acceptance. Typed terminal evidence must equal those recomputed values.
3) Anchor PID/PGID/start/finish and other transition facts cryptographically to the trusted append that introduced them. Add closed versioned detached Ed25519 event authentication (sign canonical event hash + intent/sequence domain) using current server signing key; verify with pinned server public key on every load/startup/callback/cancellation. Ordinary EventHash remains deterministic, but consistently rehashed DB rewrites without valid signature fail. Do not expose private key to journal bytes. Test fixtures use explicit deterministic test authority; no nil production bypass. Ensure private-key zeroization/Close ordering unchanged. If key lifecycle requires signer SPKI/root ID in auth, bind and validate exact current trusted key; document current single-key journal authority honestly rather than guessing rotation.
4) Cancellation outcome validation must bind audit_event_hash and completed_at exactly to the authenticated terminal event; response/evidence bindings remain exact.
5) Add multi-row correlated tamper tests: rewrite command hash in all events+evidence; relocate all five roots with valid basenames; rewrite PID/start/finish across running/terminal/evidence; raise cap/attempt; rewrite cancellation event/outcome. Recompute canonical row hashes/EventHashes but not server signature: startup/load/reconcile reject. Also prove valid signed history restart/replay.
6) Bump closed audit event/journal schema/migration as required, with exact v1-vN migration tests; do not silently accept unsigned legacy audit rows. Run focused count=5/race/full package/vet. Preserve termination/broker/evidence.