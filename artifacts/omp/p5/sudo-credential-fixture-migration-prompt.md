Mechanical test-fixture migration only. In audit_evidence_test.go, every fake production custom:sudo wrapper credential-leak fixture still reads/sets ANTHROPIC_API_KEY. Closed route now correctly exposes only SUDO_API_KEY, so shell exits before exercising leak/scrub classification. Replace those fixture script references and t.Setenv calls with SUDO_API_KEY, preserving secret strings and expected classes. Do not change audit_hard_review_test.go checks where ANTHROPIC_API_KEY is intentionally an unselected credential that must be absent. Run AuditWrapperCredentialLeak and AuditFailureAndTimeoutScrub count=10/race=3, native alias/cleanup/timeout cap/sandbox focused, full package. No production changes, no commit.