Repair ONLY production server hard-review P1 #3: independently validate complete predecessor and cancellation bindings while preserving the accepted no-raw-authority wire contract. Strict TDD; no commit/push; do not touch journal URI/schema code, Close/admission, key buffer, docs, OMP.
Design constraint: do NOT send raw repository_identity or full raw envelope. Add a closed, canonical, self-hashed wire predecessor projection containing every envelope field/binding required to reconstruct/verify it, but replace RepositoryIdentity with repository_identity_hash. Include idempotency, handoff, launch spec, fence, deadline, attempt/cap, route/source/manifest hashes, artifact/build/predecessor release/evidence identities and schema/fixed enums. Bind projection hash transitively to request hash/channel/delivery.
Server loads an operator-owned public repository policy file outside the wire mapping allowed repository_identity_hash -> raw repository identity (generic list, not Ananke-hardcoded). Owner/symlink/mode/replacement validation; command flag. Resolve exactly one mapping, reconstruct the full ExternalSupervisorEnvelope, Seal it and require exact DurableEnvelopeHash. Then call the same full authorization verification used by Client, including frozen predecessor pins and dynamic attestation/approval/MoA.
For reconcile/cancel validate exact reconstructed envelope, durable authenticated receipt, handoff ID, attempt, receipt identity, envelope hash, operation exclusivity. Multiple differently sealed handoff IDs for same receipt must conflict. Never trust merely nonempty HandoffID.
Tests: every projection field mutated one at a time with outer request/channel hashes recomputed must reject before signing/journal; unknown/duplicate repository hash policy; raw repository absent recursively; policy symlink/mode/replacement; cancellation handoff/attempt/receipt/envelope drift; same-UID canonical public-data forgeries; restart replay remains exact. Update production command/server tests for policy flag. Run focused repeated/race, full compile/vet/contracts if time.