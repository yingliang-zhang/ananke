Mechanical fix with proven root cause. In parseAuditTimeoutEvidence, strict cwd comparison must handle Darwin logical /var versus physical /private/var. Validate BOTH reported cwd and expected invocation.WorkDir are absolute and filepath.Clean-equal to themselves, then canonicalize both with EvalSymlinks (fail closed if canonicalization fails) and compare canonical physical paths. Apply same two-sided safe canonical comparison to session_path if needed, while still requiring the reported value names exactly the intended existing stable session root and cannot traverse. Do NOT relax other exact 8-line/UUID/deadline/marker requirements. Remove all temporary AUDIT_TIMEOUT_* fmt.Printf instrumentation from audit_evidence.go and audit_runtime.go and clean unused imports. Run the two timeout tests count=10 and race count=3 plus strict parser negative tests. No other changes, no commit.