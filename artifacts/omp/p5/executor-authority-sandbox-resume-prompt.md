Resume exact session 019f9a9a-3fac-7000-9053-d6e2d7ebe97d. Finish ONLY current authority/sandbox batch based on observed focused failures.
1) sandbox-exec does not preserve ExtraFiles reliably. Execute exact opened wrapper bytes through stdin: set command.Stdin to the O_NOFOLLOW/hash-validated wrapper FD and invoke immutable /bin/bash as `-s -- <wrapper args>` under sandbox-exec. This must execute the already-open descriptor across pathname replacement; no path fallback. Update descriptor hash/argv tests.
2) Make least-authority Darwin profile actually run bash/fake wrapper: add only empirically required macOS mach/bootstrap/sysctl operations/services and canonical /private aliases/executable mapping. Do not restore broad process*, mach-lookup, file-read*, or unrestricted outbound. Preserve adversarial read/parent/signal/local-socket denials. Use short /tmp socket path to respect Darwin sun_path limit.
3) Fix cleanup-aware tests, not production weakening: output-tamper test must arrange a retained verified-success artifact before tamper; failure/nonzero/evidence-reject tests should assert the actual closed failed terminal then tree deletion, not wait for waiting_for_human unless attempt-cap semantics requires it. Secret-bearing timeout must scrub and end fail-closed; clean exact timeout may preserve session.
4) Keep policy rehash, route credential env, cleanup, atomic readiness intact. Run focused failing tests, batch tests repeat/race. No evidence/resume/cancel redesign, no real OMP, no commit.