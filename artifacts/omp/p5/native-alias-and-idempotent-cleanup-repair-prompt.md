Repair two proven regressions after OMP native isolation. Strict TDD; no real provider; no commit.
A) Darwin native write-deny alias bypass:
- auditSandboxProfile currently emits native `(deny file-write*)` only for `/var/...`; allow rules use sandboxPathVariants including `/private/var/...`. Tests exit 75/91 because child can mkdir through canonical alias.
- Emit exact deny subpath + exact addon literal for EVERY sandboxPathVariants(nativeVersionDir/nativeAddonPath), deduplicated. Add tests that mutation/mkdir via both `/var` and `/private/var` aliases fail while ordinary TMPDIR writes succeed. No broad deny that blocks needed isolated HOME state; no network/mach expansion.
B) Idempotent trusted cleanup after native transport validation:
- runAuditInvocation nonzero/error defer may already cleanup all; runtime cleanupOwned calls cleanupAuditInvocationAll again. Timeout transient cleanup also removes prompt/output/temp before final cleanup. New unconditional validateAuditInvocationTransport on the second call sees missing models/native and returns artifact_cleanup_failed.
- Make cleanupAuditInvocationTransient/All a closed state machine: if all relevant transient roots exist, validate transport first then scrub; if all transient roots are already absent, treat as previously-confirmed cleanup and continue cleaning any remaining session/work roots + validate original pinned source addon; if a partial subset exists or any unexpected type/symlink exists, fail closed. Never skip validation when transport artifacts are present. Cleanup remains repeatable and scrubs secrets.
- Add direct tests: first cleanup validates and removes; second cleanup succeeds; timeout transient then full cleanup succeeds; partial missing models/native/temp fails; source pinned addon mutation fails.
- Restore expected terminal classes for nonzero/credential/malformed timeout/evidence rejection and `attempt_cap_exhausted`, not artifact_cleanup_failed. Credential bytes absent and roots empty.
Run focused tests count=10/race=3 including the two Darwin sandbox tests, all cleanup/evidence/timeout cap tests, then full internal package. Preserve installed provider-free probe PASS and exact runtime rules.