Repair ONLY the remaining production server P1 #4 plus the two documented gaps from /tmp/ananke-p5-production-server-hard-review-output/review-output.md. Strict TDD; no commit/push; do not touch journal/projection/auth protocol/OMP.
1) Close/admission race: introduce one synchronized server lifecycle gate/state. No accepted connection may be tracked or WaitGroup.Add after shutdown begins. Add must happen while holding the same gate that Close uses to mark closing; Close then closes listener, closes tracked connections, waits, and only afterward releases journal/signing/directory resources. Serve must close any accept completed after closing. Prove no Add-after-Wait, no worker uses closed resources, Close idempotent/concurrent, accept-before/after-close interleavings with deterministic hooks, race -count repetitions, socket cleanup/replacement safety.
2) Private buffer handling/proof: avoid immutable Go string copies of private key text where practical. Implement strict custom JSON decoding of the private_key quoted hex directly from raw []byte into mutable decoded []byte/ed25519.PrivateKey, while retaining closed schema/unknown-field rejection; zero raw file bytes, encoded private field bytes, decoded intermediates, and key on every error/Close. Tests retain aliases BEFORE Close/error cleanup and assert zeroization, so they are not vacuous. Ensure no private values in errors/logs/binary/journal/WAL/SHM/public bundle.
3) Update docs/local-trusted-supervisor-transport-runbook.md and current P5 ledger section honestly: production signed Unix server now exists; required --repository-policy, --trust-bundle, --private-key-bundle, --journal, socket/client UID flags; callback remains audit_not_run/waiting_for_human; no OMP/executor yet. Remove stale “no production server” claims.
Run focused lifecycle/key/docs tests repeated/race, server package/full compile/vet/contracts if time. Report exact RED/GREEN.