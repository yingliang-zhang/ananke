Fix one deterministic TEST-INFRA race only in internal/lifecycle helper tests. Root cause: writeResultFile uses os.WriteFile directly; it creates/truncates result path before writing, while waitForResult returns on first successful os.ReadFile, so under full-suite load readResultFile can observe empty bytes and JSON unmarshal fails. Strict TDD: add a deterministic regression proving readers never observe an empty/partial published result while helper writes. Change writeResultFile to marshal first, write mode-0600 temp file in same directory, close/handle errors, atomically rename to result path; clean temp on errors. Since helper cannot t.Fatal, use a robust fail-fast behavior consistent with TestMain helper mode. Do not change production or P5 files. Run regression, TestLaunchWorkerStartsPausedDistinctGroupLeader count=50 and race count=20, then lifecycle full once if time. No commit/push.