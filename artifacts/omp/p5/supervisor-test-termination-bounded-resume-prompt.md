Resume exact session 019f9baa-a693-7000-a589-0c8393d809fc. Fix only observed focused failures, preserving bounded termination design:
1) TestAuditExecutorRestartFailsClosedOnWrongPIDStartIdentity now gets ErrDeadline from bounded unconfirmed join. Wrong/reused PID must fail closed quickly without signaling unrelated process; do not wait on a waiter that cannot belong to the validated identity. Persist waiting_for_human process_identity_mismatch and return bounded, not ErrDeadline for this known mismatch path.
2) TestSupervisorAuditTestContextShutdownUsesOwnedTermination observed TERM then KILL. This is legal if child ignores TERM; update assertion/fixture to deterministically choose TERM-success OR expect exact TERM→KILL confirmed exit. Never require TERM-only nondeterministically.
3) ProductionServerSupervisorTestTerminationFailure fixture never reached supervisor-owned test: wrapper output/evidence failed first. Make fake wrapper emit valid canonical model report so supervisor test starts, then inject its kill/wait failure. Assert waiting_for_human with typed supervisor-test termination failure, no completed/cancelled/timed_out, resources retained and Close retry.
Run compile then exact 3 tests count=10/race=3 and SupervisorAuditTest|Unconfirmed|Termination suite. No policy-history edits, no real OMP, no commit.