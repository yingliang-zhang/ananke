Repair ONLY P5 hard-review blocker 4: real command/store/lifecycle/signed-peer E2E and deleted-coverage restoration. No commit/push; no production OMP/server/repair.
1) Compare current tests against HEAD using git show. Restore every materially deleted P3f store/lifecycle test or provide equivalent stronger authenticated coverage. Do not leave broad callback drift, concurrent submit/recover, reclaimed/stale fence recovery, production/fake isolation, restart replay, callback/cancellation conflict untested. Avoid weakening assertions.
2) Add a true separate-process test-only signed Unix server (re-exec test binary helper is acceptable only in _test.go). It owns Ed25519 private keys exclusively in the child test process, accepts canonical hash-only frames, verifies/answers signed delivery/reconcile/cancel.
3) Exercise production composition end-to-end: command executeInvocation -> real lifecycle runtime -> real Store/SQLite full fence -> real trustedsupervisor.Client -> separate signed process. Cover submit, process restart/reopen Store, recover, cancel; exact durable envelope/frozen release pins; receipt/callback/cancellation persistence; current/reclaimed/stale full fence behavior; same-UID wrong-key/path replacement denial; zero raw authority on wire.
4) Restart replay tests must use the real Ed25519 client/authenticator, not externalSupervisorPersistentAuthenticator.
5) Update stale runbook to actual --trust-bundle/--peer-pid command and accurately call the server test-only/separate-process. Do not claim deployable server exists.
Run focused E2E repeatedly and under race, then full/race/vet/P3d-P4 contracts if time. Report coverage restored, exact commands/results, residual absence of production server/OMP.