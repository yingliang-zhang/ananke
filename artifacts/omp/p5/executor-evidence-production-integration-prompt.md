Repair ONLY final hard-review blockers 1/2 (typed evidence production integration + central journal invariants) and related tests/docs accuracy. Read /tmp/ananke-p5-read-only-executor-final-hard-review-output/review-output.md. Strict TDD; fake wrapper only; no commit/push.
1) Production completion integration in audit_runtime.go: on exit 0, read bounded output, call decodeAuditModelReport; malformed/plaintext/unknown/noncanonical must fail. Then call runSupervisorAuditTests with the validated snapshot/invocation/policy; populate TestsRun ONLY from parent-owned closed argv results. Delete all authority from [OMP_SESSION]/[OMP_TEST]/[OMP_EVIDENCE_COMPLETE] or arbitrary session files. Session files may be diagnostic only and never affect completion/evidence/tests. Generate typed auditEvidenceReport from parent-known process result + typed model report + trusted tests. Call verifyAuditOutputUnchanged immediately before journal completed persistence. No completed event/callback if any step fails.
2) Replay/callback authority: completed EvidenceJSON embeds the exact typed model report and supervisor-owned test results plus all intent/event bindings, so replay after owned output cleanup validates journal-only immutable evidence, never mutable/deleted output. Before callback construction/replay decode+reseal typed evidence and cross-check report/output/process/test hashes and all intent bindings. On live callback, recheck output if still present, but absence after confirmed cleanup is allowed only because complete evidence bytes are journal-bound.
3) One central validateAuditExecutionHistory(intent,events) (or equivalent) used by journal append, content validation/startup, load, cancellation completion/outcome validation, reconcile/callback and replay. Enforce: derived event ID+sequence; event hash; Attempt 1..cap; legal exact state transitions; command descriptor continuity prepared→running→terminal per attempt; PID/PGID/start identity zero/set/continuity; attempt-scoped prompt/output/tmp paths and stable work/session path continuity; monotonic RFC3339Nano timestamps; terminal exit/output/session fields; typed evidence/hash schema and every cross-binding; cancellation outcome audit_event_hash and completed_at equal exact terminal event. Integrity-clean canonically rehashed tamper must fail.
4) Replace old journal test accepting {"evidence":"bounded"} with valid typed fixture. Update happy-path fake wrapper to emit exact canonical model report JSON and remove model marker authority. Add end-to-end plaintext/marker-spoof/test-spoof rejection and output mutation at persist/callback/replay tests.
5) Correct CLI/runbook/ledger only after integration: sandboxed wrapper cannot mutate repo/snapshot; trusted supervisor destroys owned snapshot. Keep no-real-canary/no-repair/no-Run.
Run focused count=5/race, full package/vet if time. Do not touch network/wrapper freeze/termination beyond compile interactions.