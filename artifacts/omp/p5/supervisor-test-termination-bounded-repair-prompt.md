Repair ONLY third-review residual 1: supervisor-owned test termination and unbounded join. Read /tmp/ananke-p5-read-only-executor-third-hard-review-output/review-output.md. Strict TDD; no real OMP; no commit/push.
1) runSupervisorAuditTests must record exact child PID/PGID/start identity immediately after Start and route timeout/context cancellation through terminateOwnedAuditProcess with the shared operations/bounds. No direct ignored Kill, no unbounded `<-waited`. Confirm group disappearance/reap. Propagate typed kill/wait/identity failure so parent audit cannot complete and runtime persists waiting_for_human/failed, never false TestsRun/completed.
2) Replace joinUnconfirmedAuditInvocation unconditional wait with bounded lifecycle. After termination failure, persist durable nonterminal failure with exact process identity, retain journal/key/resources, and return. A retry/recovery may inspect and reap later; no goroutine blocks forever. Ensure waiter goroutine/channel cannot leak or double consume Wait.
3) Executor.Close/Server.Close remain bounded and resources retained until every child and supervisor-owned test joins; later retry completes.
Tests: supervisor test TERM success, TERM ignored→KILL, KILL error, wait timeout, context shutdown, stuck test + Close retry/resources/key alive, no TestsRun/completed on failure; unconfirmed invocation returns bounded and recovers. count=10/race=3 plus termination suite. Preserve evidence/history/broker. No docs yet.