Implement P5 real READ-ONLY OMP audit executor foundation on top of accepted production server commit 1bbc880. Strict TDD; no commit/push; tests use a fake wrapper only—DO NOT call real OMP/model yet.
Authority design:
- Add an operator-owned mode-0600 canonical execution-policy file, loaded/pinned like repository policy. Key entries by exact envelope LaunchSpecHash. Each closed entry binds task ID, target repository identity/hash, exact Git commit/object/tree, canonical git-archive SHA256, fixed read-only audit prompt-template hash/ID, exact route mapping hash, wrapper absolute path + content/stat identity, hermes provider/model, task tier, internal deadline, attempt cap, allowed test commands/hashes, output/session/work roots and policy hash. No wire path/raw prompt/credential authority.
- Existing signed/sealed envelope LaunchSpecHash selects exactly one policy entry. No caller override. Recheck policy/wrapper/repo identities at every effect boundary.
Snapshot/sandbox:
- Materialize exact commit through a pinned Git executable/closed argv into a private run root; validate archive bytes/hash and reject traversal, duplicate entries, symlink/hardlink/special/PAX ambiguity. Extract files mode read-only. Verify target repo/commit unchanged is irrelevant after archive capture.
- On Darwin launch wrapper under sandbox-exec deny-by-default file writes except isolated prompt/output/session/tmp roots; allow read-only snapshot, process execution, necessary outbound API network. Source/work snapshot must be unwriteable even by chmod/rename/symlink. Unsupported platforms fail closed for production. Tests prove mutation attempts fail and source/original repo unchanged.
OMP invocation:
- Invoke ONLY route-aware omp_with_timeout.sh with exact positional deadline,prompt,output and exact --hermes-provider/--hermes-model/--task-tier/--session-dir; explicit OMP_SESSION_ROOT. Four roots prompt/output/session/work non-overlap. No bare omp, shell interpolation, arbitrary argv/env. Minimal inherited env allowlist; credentials may be inherited by name but never stored/logged/serialized. Internal deadline plus wrapper grace; process group ownership.
Durability:
- Extend server journal with immutable audit execution intent/state/evidence: prepared/running/completed/failed/timed_out/cancelled/waiting_for_human, attempt, exact command descriptor hashes, PID/start identity, timestamps, output/session UUID/evidence hashes. Receipt remains prompt; reconcile returns waiting while running and signed completed callback only from verified evidence. Zero guessed success.
- Cancellation kills owned process group, waits/reaps, persists exact terminal evidence.
- Crash/restart: reconcile running ownership. On wrapper timeout parse/persist exact [OMP_TIMEOUT] UUID. Retry uses exact --resume UUID only, never --continue/fresh; if evidence gathering complete resume prompt says synthesize only. Attempt cap fail to waiting_for_human.
Evidence:
- Bound/canonical report with source archive hash, commit, policy/route/wrapper identities, exit status, output hash/size, tests actually run from logs only, session UUID. Bounded size; reject secret/path/raw-authority leakage in signed callback—persist private local paths only in server-private journal if necessary.
Tests:
- fake route-aware wrapper validates argv/env and emits success/timeout/malformed/secret/oversize; real subprocess + sandbox; immutable snapshot mutation; policy/wrapper/repo TOCTOU; four-root isolation; exact replay; crash/restart; timeout exact resume; attempt cap; cancel PGID; wrong PID; output tamper; no credentials/log leaks; server restart. Production build excludes fake. Update runbook/ledger honestly: fake-executor only, real OMP not yet run.
Run focused/repeat/race/full/vet/P3d-P4 contracts. No real OMP, no source modification, no Run/repair.