Repair ONLY final hard-review wrapper immutability + wildcard network blocker. Strict TDD; fake wrapper/network fixtures only; no real OMP; no commit/push.
A) Freeze wrapper bytes, not inode:
- Open O_NOFOLLOW, fstat/read/hash/size with before+after identity checks. Copy exact validated bytes into a parent-owned immutable in-memory buffer. Bash must receive ONLY those frozen bytes through a private pipe/stdin; start child with pipe reader, then parent writes frozen buffer with bounded writer lifecycle and zeroes it after completion. Never give Bash the mutable wrapper fd/path. Handle Start failure, partial write, early close, writer hang/errors boundedly. Original wrapper may be path-replaced or in-place rewritten after final read, before Start, during Start, or while Bash consumes; executed bytes remain the frozen validated buffer. Command descriptor hashes frozen bytes.
- Add deterministic hooks/tests for mutation after final hash and during delayed pipe consumption; prove original bytes execute. No test-only bypass in production.
B) Route-specific endpoint confinement using trusted local CONNECT broker:
- Extend closed execution policy entry with exact provider endpoint authority (hostname + TLS port 443) and bind/hash it. Validate endpoint against route/provider mapping (for custom:sudo use coding.sudoai.cc:443; include only explicit supported mappings), no IP/localhost/userinfo/path/wildcards/arbitrary port.
- Parent server starts an invocation-scoped loopback CONNECT broker outside child sandbox. Broker accepts only strict bounded HTTP CONNECT for the single policy endpoint, rejects all other hosts/ports/methods/headers/ambiguity, resolves/dials only that endpoint with pinned resolution result, bounded connect/idle/total time, connection/goroutine cap, byte caps as feasible, closes/reaps on every path. It never logs credentials/body.
- Child receives parent-generated HTTP_PROXY/HTTPS_PROXY/ALL_PROXY (as supported) pointing to that broker, NO_PROXY empty. These are not caller policy env values. Child sandbox network allows ONLY exact loopback broker address/port. Remove mDNSResponder, wildcard *:443, TCP/UDP 53, and every other direct egress/local endpoint. Direct provider IP, DNS, arbitrary HTTPS, unrelated local sockets denied.
- Broker cannot reach loopback/private/link-local/Unix endpoints even if DNS resolves there; pin public IP set at startup and re-use it to prevent rebinding. For test injection use explicit test-only resolver/dialer, production remains strict.
Tests: broker exact CONNECT success to fake public/test dialer, wrong host/port/method/header smuggling/duplicate Host rejected; DNS rebinding/private IP rejected; direct network denied but broker socket allowed; credential cannot reach attacker endpoint; broker cleanup/no goroutine leak; policy route endpoint mutation rejected; wrapper mutation windows. Repeat/race/full package/vet. Preserve evidence/cancel logic.