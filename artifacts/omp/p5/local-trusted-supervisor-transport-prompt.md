Implement the minimum deployable LOCAL trusted-supervisor transport slice toward real Ananke read-only project audits. Strict TDD. Inspect existing P3f/P4 contracts/runtime first. Add a production Unix-domain-socket protocol adapter in a separate package/binary boundary, injected into the existing external-supervisor transport seam. Operator-configured socket path must never enter sealed envelope/evidence or callbacks. Implement bounded framed canonical JSON delivery/reconcile/cancel, peer identity/channel-binding abstraction suitable for local Unix credentials plus pinned supervisor release/trust identity, strict deadlines/size limits, receipt/callback authentication hooks, replay/idempotency, and fail-closed errors. Tests use a separate test-only fake Unix server/process and prove endpoint/credential/raw source/path/argv/env do not cross the sealed protocol. Preserve P3f/P4 exact identities, full fence checks and waiting_for_human; no OMP execution, no child source/artifact access, no repair/run creation. Add operator runbook/ledger. Run focused/full/race/vet/contracts. No commit/push.