Implement the proven root-cause fix for installed OMP provider-free transport. Evidence from /tmp/ananke_omp_broad_sandbox_probe.py: with isolated pinned native addon and complete sudo model declaration, exact OMP v17.1.3 made exactly one local POST /v1/responses and no secret output; its extra 11434/8080/1234/mDNS discovery attempts were denied and must remain denied. Strict TDD; no real provider/credential; no commit.
1) Extend execution policy with exact OMPVersion (closed semver/string for current 17.1.3) and pinned OMPNativeAddon file identity (`pi_natives.darwin-arm64.node`). Validate owner/mode/size/hash/root/path relationship at load/every effect; source must not overlap repo/invocation roots. Bind version+addon hash into policy/command descriptor/evidence.
2) During bindAuditInvocationTransport parent reads pinned addon O_NOFOLLOW, verifies before/after identity/hash, copies exact bytes into isolated `TemporaryDir/home/.omp/natives/<version>/pi_natives.darwin-arm64.node`, mode 0400; set invocation HomeDir/NativeAddonPath/hash/size and HOME=HomeDir. Models/agent/wrapper state remain isolated. Sandbox allows read/file-map of exact copied addon but explicitly denies file-write* to native directory/addon even though other invocation temp paths are writable. Validate addon unchanged before Start, after Start/evidence/cleanup. Child/model mutation must fail.
3) auditModelsConfigBytes for custom:sudo must be exact closed YAML: provider sudo, loopback baseUrl /v1, `apiKey: SUDO_API_KEY` (environment NAME only), api openai-responses, and explicit gpt-5.6-sol model fields verified from current non-secret schema: id/name/api/reasoning/input text/contextWindow 372000/maxTokens 65536/omitMaxOutputTokens true/compat supportsReasoningEffort+supportsDeveloperRole. Reject provider/model/credential-name mismatch; never copy ambient models.yml or secret value. Add equivalent closed anthropic only if fully specified and tested; otherwise fail unsupported.
4) Provider-free preflight accepts explicit ANANKE_PINNED_OMP_NATIVE_FIXTURE and uses same parent copy/profile/model builder; no HOME=/var/empty. It must assert one local POST, exact host/path, denied discovery does not cause failure, no secret in output. Do not allow direct DNS/mDNS/11434/8080/1234.
5) Use existing restrictive production profile first. If it still SIGTRAPs after native fix, capture exact sandbox deny evidence before any permission change; do not guess or add broad mach/file rights.
Tests: native replacement/mutation, copied-addon immutability, complete closed model config, exact env, installed provider-free test with `/opt/homebrew/Cellar/omp/17.1.3/bin/omp` and `/Users/yingliangzhang/.omp/natives/17.1.3/pi_natives.darwin-arm64.node`, count=10; wrapper/gateway/full package/race. No real provider.