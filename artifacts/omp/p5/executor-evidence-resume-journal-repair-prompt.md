Repair ONLY hard-review blockers 4,5,7 from /tmp/ananke-p5-read-only-executor-hard-review-output/review-output.md. Strict TDD; no commit/push; fake wrapper only; do not change authority/sandbox/cleanup/cancellation beyond interactions required.
A) Closed model report + parent-owned evidence:
- Require output file to be exact closed canonical JSON schema (not arbitrary markdown/nonempty bytes): schema version, verdict enum, bounded summary, ordered closed findings (severity/code/path hash or repo-relative safe path/line/message), no unknowns/duplicate keys/floats/raw authority/secrets/absolute paths. Canonical re-encode byte equality.
- Delete all inference of tests/evidence/session from arbitrary model-writable session files/markers.
- Parent Go supervisor, not model/wrapper, generates the canonical audit evidence report after process exit. It binds intent/envelope/policy/route/wrapper/source archive+commit, command descriptor, exit status, model-report hash/size/verdict, sandbox hash, attempt, and trusted verification results.
- Execute policy allowed tests as supervisor-owned closed argv commands after model success, under read-only snapshot sandbox with credentials removed, executable descriptors pinned, bounded timeout/output. Record exact command hash, exit code, stdout/stderr hashes/sizes in typed evidence. Model cannot claim tests. If allowed test cannot run/pass, no completed callback.
- Call verifyAuditOutputUnchanged immediately before evidence persist and immediately before callback construction/replay. Journal completed event EvidenceJSON must decode/reseal exact typed evidence and cross-match every event/intent field.
B) Stable exact resume:
- Derive session root from immutable intent, stable across attempts/resume. Attempt-specific prompt/output/tmp may differ but same OMP_SESSION_ROOT and --session-dir must persist.
- Strict timeout parser accepts exactly one wrapper-owned [OMP_TIMEOUT] record with exact ordered required keys/values at a bounded suffix; reject duplicate marker/session_id, extra/unknown lines inside record, malformed UUID/path/deadlines. Do not infer synthesis-complete from arbitrary substring. Never --continue/fresh. Exact UUID retained and --resume uses same stable root. Only add “Do not call more tools; synthesize...” when a trusted parent state says evidence gathering complete; otherwise normal exact resume.
C) Journal invariants:
- On append/startup/load/replay/callback validate command descriptor equality prepared→running→terminal, immutable private path continuity, attempt <= intent cap, derived event ID/sequence, monotonic timestamps, PID/PGID/start identity continuity, terminal exit/output/session/evidence consistency, typed evidence schema/hash and cross-bindings. Integrity-clean canonically rehashed tamper tests for each field.
- Malformed output test must fail; remove any test accepting {"evidence":"bounded"}.
Tests: marker spoofing/session spoofing, malformed/unknown JSON, output TOCTOU, model fake test marker cannot claim test, real supervisor-owned fake test command, test failure, stable resume root and strict timeout variants, cross-event tamper/startup/replay/callback. Repeat/race/full package/vet. No real OMP.