Repair ONLY third-review residual 3: exact real route-aware wrapper compatibility, without invoking a real provider. Read /tmp/ananke-p5-read-only-executor-third-hard-review-output/review-output.md and actual wrapper /Users/yingliangzhang/.hermes/profiles/orchestrator/scripts/omp_with_timeout.sh. Strict TDD; no external wrapper edits; no provider/API call; no commit/push.
A) Writable bounded state:
- Set parent-generated OMP_WRAPPER_STATE_DIR under invocation TemporaryDir/wrapper-state.
- Set PI_CODING_AGENT_DIR under TemporaryDir/omp-agent and create exact closed owner-only models.yml there before Start. Bind its canonical bytes/hash into command descriptor/evidence and validate unchanged at effect boundaries. Set OMP_SESSION_ROOT/session-dir to the stable invocation session root. HOME remains /var/empty.
- Pin exact OMP executable identity/root in execution policy, validate each effect, add only its exact root to PATH/sandbox exec/read. Wrapper uses plain `omp`; prove PATH resolves exactly the pinned file. Fail closed on replacement/mutation. No ambient ~/.omp or ~/.hermes access.
B) Explicit installed OMP transport routing; do NOT rely on HTTP_PROXY/HTTPS_PROXY/ALL_PROXY/Bun env behavior:
- Replace/extend CONNECT-only broker with a parent-owned loopback HTTP reverse gateway. models.yml provider override for the exact selected provider (custom:sudo→provider sudo) sets baseUrl to http://127.0.0.1:<gateway>/v1. Child sandbox allows only exact loopback gateway port and no other egress/DNS.
- Gateway accepts only exact bounded provider API requests needed by the pinned route (for sudo, POST /v1/responses), strict Host=its private listener authority, strict header grammar/no smuggling, bounded body/connections/time. It dials only the pre-resolved pinned public provider IP, establishes TLS with ServerName coding.sudoai.cc and normal certificate verification, rewrites Host to exact provider endpoint, strips hop-by-hop headers, streams bounded response, never logs auth/body. Reject other method/path/host/port/private/rebinding. Keep CONNECT support only if independently required; child must use explicit baseUrl gateway path.
- Add a provider-free exact-installed-transport probe: launch the policy-pinned OMP binary with isolated PI_CODING_AGENT_DIR/models.yml and fake local gateway; fake returns deterministic auth/protocol failure after recording request. Prove exact OMP sends its POST to the configured gateway and makes no direct provider/DNS connection. This may be an explicit preflight harness/test skipped only when no pinned OMP fixture is supplied; run it here against installed v17.1.3 with no real credential/provider. Test must never print credential.
C) Exact real wrapper timeout protocol/channel:
- Parse wrapper's actual output-file suffix from lines 936-967: exactly one [OMP_TIMEOUT], then exact 7 fields (8 lines total): timeout_source, internal_deadline_seconds, hard_deadline_seconds, cwd, resolved session_id, session_path (exact path first attempt or exact literal `provided by --resume` on resume), recovery_hint exact UUID. No invented source/deadline/[OMP_TIMEOUT_END] schema and do not parse captured stdout/stderr.
- Authority: frozen validated wrapper is the final writer after OMP exits; require wrapper exit 124 and confirm entire owned process group gone before parsing. Exact closed canonical model output cannot contain marker; duplicate/malformed/unresolved/candidate-count/trailing bytes reject. Bind internal deadline and hard=internal+wrapper grace, physical cwd, stable session root, exact UUID. Add first-time timeout/resume timeout fixtures generated by the real schema.
D) Wrapper admission dependencies: sandbox allow exactly the root-owned system commands/files wrapper actually uses (bash, python3 if root-owned pinned, ps, cksum, awk, wc, tr, cat, mv, date, mkdir, mktemp, grep, sleep, kill, etc.) and its private state/session/config paths—no broad process/file/network wildcard.
Tests: state dir/home isolation, models.yml mutation, OMP binary mutation/PATH shadow, gateway routing/smuggling/TLS pin, exact installed provider-free preflight, timeout exact suffix first/resume/spoof/unresolved, full wrapper fake tests. Repeat/race/full/vet. Preserve signed history/termination/evidence.