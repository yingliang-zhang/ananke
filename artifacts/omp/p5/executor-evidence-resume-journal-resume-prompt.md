Resume exact session 019f9ab9-23bf-7000-bea7-a8c3e9da01a6. Finish ONLY current evidence/resume/journal batch based on focused failures.
1) Supervisor-owned pinned test command is denied because its temp executable is not represented in the test policy/sandbox executable roots. Preserve production least-authority: policy validation must require every AllowedTest executable be within a pinned executable root and descriptor-validated. Update fixtures to include the temp executable root (or use root-owned /usr/bin/true where suitable). Do not broadly allow temp.
2) Timeout server fixtures still emit the old marker. Update fake wrapper to emit exactly the new ordered [OMP_TIMEOUT] suffix with required source/deadline/cwd/session_id/session_path/recovery fields matching invocation/stable session root. Tests must prove exact resume same OMP_SESSION_ROOT and --resume UUID. Keep malformed/duplicate/extra tests rejected.
3) Run the currently failing tests, focused evidence/resume/journal suite repeated/race. Do not touch cancellation batch or real OMP. No commit/push.