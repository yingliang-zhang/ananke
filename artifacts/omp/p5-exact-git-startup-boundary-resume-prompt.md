Resume the exact P5 Git-startup implementation session. Preserve all current edits. Do not restart or redesign.

The narrow implementation and RED evidence exist, but the focused GREEN test is still RED:
`TestDarwinDirectOMPSandboxExecutesPinnedGitWithoutParentRepositoryDiscovery` exits fake OMP code 70 with no stdout/stderr. The environment test passed in the two-test rerun. Continue from the exact existing state.

Diagnose the failing fake fixture systematically. Add only a bounded test-only closed stage code if needed; never emit credentials, environment values, paths, Git output, or raw model/provider data. Determine which exact probe fails. Likely candidates must be tested one at a time: pinned Git startup, repository ceiling, global/system config isolation, source/ref mutation denial, or shell/arbitrary executable denial.

Do not broaden production permissions beyond exact `entry.GitExecutable.Path` literal read/map/exec. Do not add `/usr/bin` subpath, shell, broad file, sysctl, parent-directory, home, or network authority. Keep the fixed Git environment contract and command-descriptor v6 binding.

Finish the requested scope:
1. Make the tight Git startup test GREEN without weakening its assertions.
2. Ensure the test proves parent repository non-discovery, ignored ambient Git config, snapshot and parent HEAD immutability, and shell/arbitrary executable denial. Correct a test/probe mistake if evidence proves the production policy is already right; do not change production speculatively.
3. Run the focused GREEN suite once, then `-count=10` if green.
4. Run scoped gofmt verification and `git diff --check`.
5. Do not run full package/race/vet/canary; do not edit ledger/P6, commit, or push.
6. Return exact RED/GREEN commands/results and changed files.

If time becomes short, prioritize getting the tight test and focused suite green; leave a precise summary rather than broad additional work.