Repair P2c generator determinism/quality blocker. Current state: cargo fmt --check passes only after formatting generated Rust, then npm --prefix gui run check:renderer-public fails because generator emits unformatted Rust; generated P2c validators produce 18 dead_code warnings. Modify gui/scripts/generate-renderer-public.mjs (and generated outputs via generator) so generated Rust is rustfmt-stable byte-for-byte and emits only helpers actually used per DTO (or otherwise no warnings), preserving deny_unknown_fields and P2c semantic validation/tests. Never hand-edit generated output as final state. Run generate then check renderer-public, cargo fmt --check, cargo test all-targets with no warnings, TS/privacy/decoder/P2c gates. No Tauri/UI/daemon scope, no commit/push. Ledger factual RED/GREEN.