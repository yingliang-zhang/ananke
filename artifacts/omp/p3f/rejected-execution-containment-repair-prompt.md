Repair rejected P3f production execution runtime. Strict TDD. Move fake wrapper execution containment entirely to _test.go / test-only build; production must retain only inert typed preparation and no launch capability. Hard-bind fake execution to a fixed test binary/artifact and compiler GoFiles guard; no dynamic approval digest/self-consistent arbitrary artifact acceptance. Remove FD-to-path production launcher surface. Fix owned wrapper cleanup so descriptors always close even if inode/path replacement prevents deletion; add replacement cleanup test. Preserve all P3f fake sandbox/fence/FD output tests. Document that real production execution remains blocked pending a separate approved exec-by-FD/independently trusted artifact design. No real OMP/target/commit.