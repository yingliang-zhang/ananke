Repair final P3f trusted protocol review blockers. Design-only/no network/commit. Keep the authoritative protocol contract explicitly EXACTLY 37 cases everywhere authoritative while retaining unrelated Darwin macOS 27 references. Add strict identifier validation for release_approval.approval_id and moa_typed_role_grant.grant_id (safe opaque identifier grammar only; reject URLs, whitespace, secret/key markers, arbitrary raw authority). Add canonical red-flag vectors and rehashed self-test mutations for endpoint/secret payload in both IDs, assert all still waiting_for_human. Verify the exact 37-case P3f protocol inventory and run P3f/P3d chain.