Repair P3f pinned Git archive provenance. Strict TDD/test-only fake runtime/no real OMP/target/commit. Do not treat PAX comment as authenticated provenance. Bind an immutable archive SHA-256 to activation/manifest and verify it before staging, while retaining PAX commit as consistency metadata only. Add negative test: hand-built non-Git tar with expected PAX comment and exact expected member names/content hashes but different archive bytes must fail before fake execution. Update manifests/fixture binding if necessary; preserve FD/sandbox/fence/fake-only boundaries. Run focused/full/race/vet/contracts/ledger.