Repair P3f production-wrapper identity core review blockers. Strict TDD/no wrapper execution/commit. Pin the exact canonical P3d source snapshot SHA-256 from contracts/p3d and validate SHA-256 format (positive test must use canonical value; malformed/bad-length/other drift reject). Expand AST static guard to reject prohibited execution/argv/path/environment surfaces: os/exec, os.StartProcess, syscall.Exec, os.Args, os.Environ and structs/fields named argv/env/path/program/command/executable (case normalized). Preserve inert FD-only core. Run focused/full/race/vet/contracts/ledger.