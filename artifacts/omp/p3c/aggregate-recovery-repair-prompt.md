Repair P3c aggregate recovery blocker. Strict TDD; no real Run/worktree/process/adapter/OMP/daemon/Tauri/UI/commit. Make aggregate store/lifecycle recovery preserve per-active launch-spec hash/cause instead of fail-all on one corrupt boundary. recoverAll must continue valid exact retry actions and emit per-corrupt `WaitingForHuman` action with exact hash and private ErrLaunchRecordCorrupt, no terminal/evidence/process inference. Add tests valid+corrupt active aggregate, assert valid action retained/corrupt action present/runs empty; add explicit evidence-intent recovery fail-closed test. Keep single-launch semantics. Run focused/full/race/P3 contract gates and ledger evidence.