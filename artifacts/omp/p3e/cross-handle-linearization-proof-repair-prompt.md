Repair P3e missing deterministic cross-handle final-fence proof ONLY. Strict TDD/fake-only/no real OMP/target/commit. Implement test hooks/barriers sufficient to pause inside WithLaunchFenceAdmission callback after final descriptor/fence/outbox validation and before p3eExecAdapter.Start. Open a second Store handle on same DB; start reclaim goroutine through it; prove it cannot complete while admission immediate transaction held; release fake start, prove invocation occurs, then reclaim completes. Also convert/add second-handle pre-admission reclaim case proving if reclaim commits before transaction acquisition fake never invokes. Do not weaken production linearization or add production runtime scope. Run focused repeated/full/race/vet/contracts/ledger.