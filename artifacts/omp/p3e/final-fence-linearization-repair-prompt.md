Repair remaining P3e post-final-validation stale-fence launch interval. Strict TDD/fake-only/no real OMP/target/commit. Inspect current adapter start path and reviewer finding. Ensure reclaimed fence cannot invoke fake even if reclaim occurs after existing final validation but before OS exec. Introduce a correct linearization/admission mechanism using existing durable authority or an in-process testable gate that is revalidated immediately at executable invocation without a race window; do not claim a mutex alone solves cross-process fence. Add deterministic interleaving test proving no fake invocation after the precise final-validation boundary reclaim. Preserve all repaired seal/descriptor/cancel/JSON behavior. Run focused/full/race/vet/contracts/ledger.