Repair P3e deterministic cross-handle proof quality ONLY. Strict fake-only/no real OMP/target/commit. Existing 25ms noncompletion assertion is insufficient. Create a testable second-handle actual SQLite admission attempt while first WithLaunchFenceAdmission transaction is held. Prefer a zero-busy-timeout/explicit BEGIN IMMEDIATE observable SQLITE_BUSY path or a narrowly test-only BeginTx hook emitted immediately before real BeginTx. Assert second handle actually attempted DB write admission before fake start; it must receive SQLITE_BUSY/block observably while first transaction held; after release/rollback reclaim succeeds and generation increments. Keep pre-admission second-handle reclaim case. Avoid weakening production isolation and record precise evidence.