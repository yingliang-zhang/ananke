Repair P3e final cross-handle test gap ONLY. Fake-only/no real OMP/target/commit. In the held WithLaunchFenceAdmission callback after final validation/before fake Start, invoke actual second-handle ReclaimLaunchClaim (not raw BeginTx) with connection busy_timeout=0; assert its returned error has observable SQLITE_BUSY / sqlite locked semantics and fake has not run. Then release fake/admission rollback, restore/retry exact ReclaimLaunchClaim and assert success/fence generation+1. Retain pre-admission reclaim case. Ensure repeated proof runs isolated from full/race runs; do not claim determinism from a run concurrent with broader suites. Run exact focused repetitions/full/race/vet/contracts/ledger.