Continue the exact sealed-HOME implementation after timeout. The RED tests were added and failed on missing `HomeStateDir`/`HomeRunDir`; implementation and gates are incomplete. Do not merely synthesize, and do not add more broad exploratory reads. Use the source context already gathered, implement the descriptor-authenticated `.omp` 0500 + `.omp/run` 0700 contract, sandbox exact-state protection, owned-root validation/cleanup, provider-free preflight update, and tests from the prior prompt. Preserve all dirty work; no real canary, ledger edits, commits, wrapper edits, reset/clean/restore, schema bump, or artifact allowlist. Run focused GREEN and as many bounded gates as time permits. Return exact remaining work if another deadline is reached.