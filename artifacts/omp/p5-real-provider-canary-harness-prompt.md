Implement a build-tagged real-provider canary harness in `/Users/yingliangzhang/Projects/ananke-p0a-schema-codegen` without running a real provider call. No commit, no docs/ledger. Scope only `internal/trustedsupervisor/*real*canary*_test.go` plus the minimum existing test-helper refactor needed; do not change production behavior.

Requirements:
- File gated by `//go:build ananke_real_provider_canary` so default tests do not compile or run it.
- Test name `TestAuditRealProviderCanary` and require explicit `ANANKE_REAL_PROVIDER_CANARY=1`. Otherwise skip.
- Require but never print: `SUDO_API_KEY`. Require absolute clean env paths for `ANANKE_REAL_CANARY_REPOSITORY`, `ANANKE_REAL_CANARY_WRAPPER`, `ANANKE_PINNED_OMP_FIXTURE`, `ANANKE_PINNED_OMP_NATIVE_FIXTURE`. Error/skip messages may name missing variable but never value.
- Exercise the production P5 path as fully as test authority permits: exact current repository commit/tree and deterministic git archive; materialize immutable snapshot; pinned real route-aware wrapper, OMP 17.1.3 executable/native addon/runtime authority; real dual-stack local gateway to the allowlisted custom:sudo endpoint; real Darwin sandbox; fixed prompt; typed canonical model output decoder; one supervisor-owned pinned read-only test; evidence construction and cleanup/finalization. Prefer the real audit executor/journal lifecycle over directly calling OMP. Compile-time test runtime authority may replace only root/UID namespace provisioning; no other production check bypass.
- The canary must audit the supplied repository itself, not a toy repo. It must prove original repository git status and exact tracked bytes/hash are unchanged before/after. It must not commit, push, create branches, or write to source. Work/prompt/output/session/tmp roots must be new isolated temp roots and scrubbed after confirmed exit; preserve only a bounded sanitized evidence summary under `t.TempDir`/test logs.
- Do not print model raw output, credentials, absolute private paths, full prompts, or source. Log only verdict, finding count, evidence/report hashes, test IDs/exit, and terminal state. Reject any leak.
- Deadline max 600s, attempt cap 1 for canary. No automatic resume. Any timeout stays nonterminal/fails the test without inference.
- Add a provider-free compile/smoke test in a normal `_test.go` that invokes the canary binary/test without opt-in and proves it skips before credential/path lookup. If Go build tags make that impractical, at minimum run `go test -tags ananke_real_provider_canary ... -run '^TestAuditRealProviderCanary$'` without opt-in and verify SKIP.
- Verify default package tests still pass and tagged no-opt-in test SKIPs. Do not read credential files. Return exact files/commands/results and the one future command shape using env variable NAMES with placeholder values, never secret values.