Resume exact session 019f9e0a-66cb-7000-a0bc-d3bcb5f11649. Do not inspect more repository files or expand scope. You already collected context and created the fake adapter test. Implement the production `internal/repairrunner` package and complete Tasks 3-5 now. Use the newly available `Store.GetP6RepairEventHead` API. Keep all edits under `internal/repairrunner/**`.

Prioritize a coherent minimal implementation over additional edge cases: durable prepared-only admission; append running before effect; restart running denies without adapter call; pinned `/usr/bin/git`; clean original and exact base; detached worktree; fake adapter test-only; strict allowlisted regular tracked modification; reject untracked/rename/delete/symlink/gitlink/mode/binary/oversize/outside path; original repo hash/status unchanged; retain worktree; typed candidate with diff hash/size/paths. Known error → failed, uncertain identity/repo drift → waiting_for_human. No tests/evidence finalization yet and no waiting_for_review in this phase.

Run focused count=1 and then the broadest count/race tests that fit the remaining deadline, gofmt/vet/diff-check. Do not commit/docs. Return exact results and explicit unimplemented edge cases.