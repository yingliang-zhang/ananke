Write a detailed implementation plan, but do not edit code, for P6a in `/Users/yingliangzhang/Projects/ananke-p0a-schema-codegen`. Save the plan to `docs/plans/2026-07-26-p6a-controlled-repair-foundation.md`. Read current P4 durable admission (`internal/store/p4_evidence_admission.go`), store migration/concurrency conventions, lifecycle test-only adapters, and experiment ledger first.

Goal: the smallest guarded MVP that materially advances using Ananke to modify a project. Implement a provider-free, test-driven controlled repair foundation:
- A durable P6 human authorization binds exact P4 admission/bundle/full fence, local GUI operator decision, exact repository identity/path, exact base commit/tree, one detached isolated worktree root, ordered allowlisted writable repository-relative paths, ordered supervisor-owned tests, attempt number 1..cap 2, and exact repair route/role identities. It must be canonical/self-hashed and insert-only. P4 remains design-only and alone grants nothing.
- Atomic admission must occur before filesystem/process effects. Exact replay returns same fact; conflict or stale/missing P4 fact denies. No raw prompt, credential, arbitrary command, environment, endpoint, or source bytes in durable authority.
- A new isolated-worktree runner uses pinned `/usr/bin/git` with closed argv/environment to create a detached worktree at exact base commit. It has an injected adapter interface. Only a fake adapter in `_test.go` edits one allowlisted file for this slice; production has no concrete repair adapter yet.
- After adapter exit, independently inspect the worktree through pinned Git. Reject untracked files, symlinks/submodules, binary/oversized diffs, path escapes, or any changed path outside the exact allowlist. Run only policy-pinned supervisor-owned tests using exact executable identity and fixed argv. Capture bounded stdout/stderr hash/size and exit evidence.
- Persist immutable attempt events prepared→running→waiting_for_review or failed. Never claim success. Evidence binds authorization/spec/P4/base commit/tree/worktree identity/diff hash+size/ordered changed paths/test results/adapter identity and attempt number. No commit, branch update, push, merge, source-repo write, repair retry scheduling, or cleanup of review worktree.
- Crash/restart before/after each effect must fail closed or resume without repeating an effect. If complete durable recovery is too large, plan a strict first sub-slice that leaves an explicit nonterminal recovery state and never reruns adapter automatically.
- Production build must prove fake adapter absent. Tests use a temporary Git repo, no provider/network, and verify original repository remains byte-identical.
- Include exact files/functions/tables/events, bite-sized RED→GREEN tasks, commands, acceptance criteria, review gates, and explicit non-goals. Prefer new `internal/repairrunner` plus minimal `internal/store` additions; avoid changing trusted-supervisor P5 code.
- No commit in plan. Return a concise summary after writing the plan.