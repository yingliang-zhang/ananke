Resume this exact Arm A session for one narrow revision-4 evidence repair. You may use tools. Do not read Arm B and do not change shared contract/fixtures or production sources.

Independent hard review returned CHANGES REQUESTED solely for these evidence gaps:
1. Extend the single reproducible verifier so it structurally proves both TypeScript AND generated Rust-public have no `token` or raw daemon `error` field. Do not use a broad word grep that fails on comments/errors; field-level checks only.
2. Add the exact protected production scope guard to verifier: `git diff --exit-code -- internal/lifecycle/engine.go gui/src-tauri/src/lib.rs gui/src/main.ts`, correctly rooted at repository root even when verifier runs under the experiment directory.
3. Update the machine-readable summary/README for revision 4 with current raw commands/results, generated file/LOC count, dependency-manifest delta/footprint, repair outcome, and fresh timings. Do not retain stale statement that revision 3 is final.
4. Remove user-specific absolute cargo/node assumptions from scripts/README. Resolve node/cargo from PATH and fail clearly if unavailable; README must give a portable command with Node 22 prerequisite.

Keep scope inside experiments/p0a/jsonschema-quicktype except read-only production scope check. Preserve all existing behavior. Run the complete verifier and git diff --check before final reply. State whether every specified repair requirement passed.